name: Build DEB Package

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          python3-all \
          debhelper \
          dh-python \
          fakeroot \
          devscripts \
          python3-stdeb

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install setuptools wheel

    - name: Build source distribution
      run: |
        python setup.py sdist

    - name: Convert source distribution to DEB package
      run: |
        py2dsc --with-python3 dist/container-manager-*.tar.gz

    - name: Build DEB package with pybuild
      run: |
        cd deb_dist/container-manager-*
        debian/rules clean
        debian/rules build
        debian/rules binary

    - name: Upload DEB artifact
      uses: actions/upload-artifact@v4
      with:
        name: container-manager.deb
        path: deb_dist/*.deb
